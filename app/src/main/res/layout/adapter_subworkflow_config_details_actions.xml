<?xml version="1.0" encoding="utf-8"?>
<layout>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/value_5dp">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/action_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/value_10dp"
            android:paddingTop="@dimen/value_5dp"
            android:background="@drawable/green_background"
            android:paddingEnd="@dimen/value_10dp"
            android:paddingBottom="@dimen/value_5dp"
            android:text="Save"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="@dimen/text_14sp" />

        <!--        android:text="@{row.action.action}"-->
    </RelativeLayout>

    <data>

        <variable
            name="row"
            type="com.apollopharmacy.vishwam.ui.home.cms.complainList.model.SubworkflowConfigDetailsResponse.Rows" />
    </data>
</layout>